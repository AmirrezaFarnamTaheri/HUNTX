# HUNTX comprehensive review — 2026-09-18

Scope: whole tracked project (backend, frontend, delivery, CI, docs, history). Evidence-first: every implemented change below was reproduced failing (or verified at source) and then verified green. Not claimed: exhaustive defect discovery, live deployment behavior, or zero remaining issues. No live ingestion, scanning, Telegram, publication or cloud calls were made.

## Verified fixes (red → green unless noted)

| # | Area | Defect (evidence) | Fix | Regression |
|---|------|-------------------|-----|------------|
| 1 | Output alias | `tests/test_b64sub_alias_contract.py` asserted only truthy bytes (`... if False else ...`), never byte identity; comment falsely claimed retirement of formats | Real byte-identity assert + successive-snapshot alias sync test; `output_ownership.py` comment now states exactly what the alias does | `tests/test_b64sub_alias_contract.py` (4 tests) |
| 2 | Frontend dialogs | Artifact-QR close path set `hidden=true` + `display:none !important` on `#modal-overlay`; the next dialog removed only a class, so it never became visible (reproduced red in Chromium) | Unified with shared `closeModal()` lifecycle; removed duplicate overlay listeners and persistent overrides | `tests/frontend_browser.spec.mjs` "artifact QR closure does not hide subsequent dialogs" |
| 3 | Production packaging | `huntx.yml` required `assets/js/bundle.js` (retired); generator bans it and the shell loads native `app.js` — every release package would fail verification | Package gate now requires `index.html`, `assets/js/app.js`, `assets/css/tailwind.css`, `sw.js` | `tests/test_frontend_delivery.py::test_production_packaging_requires_current_frontend_assets` (red→green) |
| 4 | Telegram captions | Schema default `caption_template="{filename}"` crashed `.format()` (`KeyError: 'filename'`) — reproduced | Default changed to `"Update: {timestamp}"`, consistent with publish fallback | `tests/test_dev_export_manifest_hardening.py` |
| 5 | Dev export | String `first_seen` timestamps crashed the deterministic sort (`TypeError`); manifest now drops non-numeric entries with a warning and keeps the export alive | Loader coercion in `Orchestrator._export_dev_outputs`; invariant asserted under the installed dev-manifest contract | same file (crash reproduced → fixed) |
| 6 | Publication gate | Degraded producer run can finish "success" without uploading the release artifact; publisher then failed obscurely at download time (`publish-generated-outputs.yml:167–173`) | Source resolver now lists run artifacts and fails explicitly when `huntx-output-<run>-<attempt>` is absent, before any download | `tests/test_generated_output_publication.py::test_publisher_rejects_degraded_producer_before_downloads` (red→green; workflow still parses) |
| 7 | Daemon health (Go) | IPv6 nodes dialled via `fmt.Sprintf("%s:%d")` → unbracketed address → node marked dead; PAC path already used `net.JoinHostPort` | Health dial uses `net.JoinHostPort` (+ `strconv` import) | `cmd/huntx-daemon/ownership_test.go` |
| 8 | Daemon ownership (Go) | `NewDaemon` retained caller-owned slice; health goroutine writes it under the daemon mutex while the caller may mutate the same array | Storage copied at construction | same file |
| 9 | Daemon lifecycle (Go) | Listener failure logged and left the process alive without serving (zombie) | `select` on signal vs server error; fatal listener error cancels health checks and exits 1 | verified by vet/build; runtime exit path untested offline (process-level) |
| 10 | Earlier UI round (already in tree) | Skip navigation, dialog Escape/focus return, shortcut-hint contrast, 12px readability floor | Implemented and verified before this review; 15 browser tests pass | `tests/frontend_browser.spec.mjs` |

## Review coverage (evidence ledger, not a completeness claim)

- Four parallel read-only audits produced reports in the session scratchpad: `review-python.md` (54/117 src modules deep, 63 inventory-only), `review-go.md` (12 production files + 10 deploy files fully; 33 Go files uninspected), `review-frontend.md` (delivered with gaps noted), `review-delivery-history.md` (all 7 workflows; snapshot-assembly and remaining docs partial).
- Independent empirical reproduction for every fix above; no finding was promoted from a filename or version number.
- Runtime/graph notes: `graphify-out/` is stale-by-design (gitignored); current source is authoritative. pip check: clean. No vulnerability claims from version numbers.

## Remaining known issues (unfixed, with pointers)

1. **Python (High, from audit):** production builders write public outputs before release eligibility is computed; and ingestion-residue/budget demotion happens after publication/export decisions (`hardened_orchestrator.py:282–372` vs `runtime_resilience.py:411–420`). Fix needs a design decision (move export behind the eligibility flag or re-order demotion); not safe to do blindly in this pass.
2. **Go (Medium):** probe accepts bearer credentials over plaintext remote HTTP (`cmd/huntx-probe/probe.go:119–134`); needs fail-closed config validation. Not fixed: touching probe configuration behavior was out of the bounded scope and deserves an explicit product decision.
3. **Helm (High for shared namespaces):** release-selector overlap in `deploy/helm/huntx-fleet` (G1) — needs chart-level label work plus an immutable-selector migration plan.
4. **Docs drift:** developer/conductor verification instructions lag CI; `requirements-dev.in`/`uv.lock` ownership unclear; historical reports preserved but their unresolved items need reconciliation (see `review-delivery-history.md` disposition table).
5. **Frontend:** globe label crowding and a full WCAG audit remain open; the 44 earlier impeccable warnings were never all triaged.
6. **Historical material:** several deleted reports/patches may contain unique unresolved obligations; revival candidates are listed in the delivery/history report and were NOT auto-restored (no destructive cleanup was performed either).

## Verification results (all executed this session, final state)

- Python: flake8 `--count` 0; mypy "no issues in 117 source files"; pytest `-m "not perf"` → **930 passed, 1 skipped, 4 deselected, 57 subtests passed**.
- Playwright (Chromium, local-only): **15 passed**.
- Go: `go test ./...`, `go vet ./...`, `go build ./...` → **exit 0** (scratch TMPDIR per repo recipe).
- Workflow YAML parses; `update_frontend.py --check` passes; `git diff --check` clean.
- Go network caveat: some probe/geo tests dial public endpoints by design; the suite passing here does not certify a no-network run.

## Constraints honored

No commits, no push, no deployment, no live proxy/Telegram/scanner runs, no secret disclosure, no deletion of historical material; defensive scope only. All changes remain uncommitted in the working tree.

## Continuation pass — remaining code defects fixed

The three code defects left open above were fixed in a follow-up pass and
verified red-to-green. The softer items (docs drift, frontend WCAG/globe
crowding, historical-material reconciliation) remain open by design; they
need a product or editorial decision, not a code change.

| # | Area | Defect | Fix | Regression |
|---|------|--------|-----|------------|
| C1 | Release ordering (High) | Budget exhaustion / queue residue demoted a run only *after* it had already published and replaced the public snapshot (`hardened_orchestrator.py:280`) | Added an overridable `_ingestion_demotion_reason()` hook; the optimized runtime reports `ingestion_budget_exhausted` / `ingestion_residue_remaining`, and the hardened contract folds it into `release_eligible` *before* publish/export; summary now carries `partial_reason` | `tests/test_ingestion_demotion_gate.py` (9 tests; 3 confirmed red without the fix) |
| C2 | Probe credentials (Medium) | `SubmitReport` would send `Authorization: Bearer …` over plaintext remote HTTP | `validateTransport()` fails closed before any request: https or loopback/localhost only, else an error; no token means no check | `cmd/huntx-probe/probe_test.go` (2 new tests, 8 subcases) |
| C3 | Helm selector overlap (High, shared namespaces) | Daemon + probe selectors matched on `name`/`component` (and probe `instance: <probe-id>`) only, so two releases in one namespace selected each other's pods | Every Deployment/Service selector and pod-label set now includes `app.kubernetes.io/instance: {{ .Release.Name }}`; probes keep identity via a distinct `probe-id` label | Rendered all templates with substituted values; asserted selector == pod labels and instance present (helm CLI unavailable, validated structurally) |
| C4 | Type narrowing (from earlier pass) | `npvt.py` used `type(latency) in (int, float)`, which mypy cannot narrow → 2 errors | Switched to `isinstance(...)` (excluding `bool`); mypy now clean | `tests/test_latency_remark_tolerance.py` (14 tests) |

### Verification (final state, this pass)

- Python: flake8 `--count` 0; mypy "no issues found in 117 source files"; pytest `-m "not perf"` → **1009 passed, 1 skipped, 4 deselected, 57 subtests passed**.
- Go: `go test ./...` (all packages ok), `go vet ./...`, `go build ./...` → clean (scratch TMPDIR per repo recipe).
- Frontend: `node --test tests/frontend_runtime.test.mjs` → 16 passed; Playwright → **15 passed**.
- Delivery: `update_frontend.py --check` passes; all 6 workflow YAMLs parse; `git diff --check` clean.

### Still open (unchanged, decision required)

1. Docs drift — developer/conductor verification instructions lag CI.
2. Frontend — globe label crowding and a full WCAG audit.
3. Historical material — deleted reports/patches may hold unique obligations; not auto-restored.
4. Helm — an immutable-selector migration plan for existing releases (selectors are immutable on live Deployments; the fix applies to fresh installs and requires a resource-recreation path for existing ones).

### Constraints honored

No commits, no push, no deployment, no live proxy/Telegram/scanner runs, no
secret disclosure, no deletion of historical material. CRLF byte-structure
of all patched Python files was preserved and asserted.

## Documentation finalization pass — stale docs cleared

A follow-up pass audited every tracked document (51 files) and closed the docs-drift
and historical-material items left open above. Dispositions: 31 kept as-is, 10 corrected,
10 pruned. Every correction was verified against the tree, not against a document's own
claims.

| # | Area | Defect (evidence) | Fix | Regression |
|---|------|-------------------|-----|------------|
| D1 | Snapshot assembly | `assemble_generated_snapshot.py` still named the retired `assets/js/bundle.js` in the shell-copy list; a stray local bundle would have been staged into the generated-only snapshot | List reduced to `index.html` + `assets/js/data.js` | `tests/test_generated_output_publication.py::test_retired_bundle_is_never_staged_from_the_shell` (red→green) |
| D2 | Main sync | `sync_generated_outputs_to_main.py` kept `docs/assets/js/bundle.js` in `MANAGED_DOCS_PATHS`, so a reappeared bundle could be mirrored over the native modules | Removed from the managed set; the path is now rejected like any other unmanaged docs path | `test_sync_rejects_the_retired_bundle_path` |
| D3 | Frontend export | `buildSingboxConfig` emitted route rules referencing a `dns-out` outbound it never declared plus `geosite-cn`/`geoip-cn`/`geosite-category-ads-all` rule sets the config never defines — clients must supply them or reject the config | Aligned to the shipped backend contract: `protocol: dns → action: hijack-dns`, private-IP → direct, `final: select-manual`, no rule sets | `tests/frontend_runtime.test.mjs` "Sing-box config references only outbounds it declares" (17 Node tests green) |
| D4 | README | Badge linked `workflows/ci.yml`, which does not exist (CI is `pr-validation.yml`); repo tree placed `DESIGN.md` under `docs/` though it lives at the root; the `assets/js` list omitted five live modules | Badge and tree corrected; the C4 "single source of truth" wording now keeps `docs/architecture.html` as a live companion map | link resolution verified manually |
| D5 | DESIGN.md | Certified "100% standalone zero-CORS bundle (`bundle.js`" and a "Standalone IIFE bundle" while CI enforces the bundle's retirement; listed a `clash.yaml` endpoint with no clash builder; advertised 27 artifacts (catalog: 24) | Replaced with the native-module + CDN-fonts reality; endpoints now name the real `all_sources.npvt.*` artifacts at 24 | counts regenerated from `docs/catalog.json` |
| D6 | DEVELOPMENT / USER_GUIDE | Tree listed `DESIGN.md` under `docs/`; `C4_ARCHITECTURE.md` described as the formal multi-level model though it is a pointer stub; the format table listed 12 of the 16 registered handlers (missing slipnet/tut/sks/tmt) | Trees corrected, duplicate `app.js` line removed, format table completed to 16 | handler list verified against `register_builtin.py` |
| D7 | Absorption ledger | Claimed the donor-only modules "are not imported" while `src/huntx/__init__.py` imports all five | Reworded to the accurate claim: not on the *production execution path* (runtime is built via `core.runtime_factory.create_production_orchestrator()`) | import graph verified |
| D8 | conductor gates | `conductor/workflow.md` claimed parity with PR CI but omitted the whole frontend/asset gate, `govulncheck`, two of four Go builds, the nested v2ray collector gate, YAML validation, and the Docker/Helm manifest gate | Gate list completed to mirror `pr-validation.yml` command-for-command | compared line-by-line with the workflow |
| D9 | conductor/product.md | Stated 85 production sources; `configs/config.prod.yaml` defines 81 | Corrected to 81 (1 route, 12 formats — both verified) | parsed with PyYAML |
| D10 | Historical records | Two history documents carried no supersession banner: `HARDENING_AUDIT_2026-07-12.md` (its four open findings closed by PR #61) and `MASTER_FORENSIC_COMPENDI.md` ("Production Ready" — the exact claim class the absorption ledger rejects) | Both now carry supersession banners pointing to the closing records; the next-gen track's unchecked boxes (all deliverables verified present) are checked with a delivery-status note | existence of every cited deliverable verified with `ls` |

### Pruned (10 files, no inbound links)

Superseded plans whose deliverables either landed under other names or were delivered as
helper modules: the 2026-08-17 remediation quintet (160 unchecked boxes, declared
non-normative by `2026-08-19-pr76-review-resolution.md`), the 2026-08-21 eBPF/AI-routing
and next-gen roadmap plans (all boxes unchecked, all deliverables present), both 2026-07-25
plans, and the 2026-07-25 session handoff (its "push 19 local commits" obligation is long
done). No tracked file linked to any of them; the six surviving `docs/plans/` entries are
verified closure records. No destructive edit touched a file with open obligations.

### Verification (final state, this pass)

- Python: flake8 `--count` 0; mypy "no issues found in 117 source files"; pytest `-m "not perf"` → **1029 passed, 1 skipped, 4 deselected, 57 subtests passed**.
- Frontend: `node --test tests/frontend_runtime.test.mjs` → **17 passed**; `scripts/update_frontend.py --check` passes.
- Go: `go test ./...`, `go vet ./...`, `go build ./...` → clean (scratch TMPDIR per repo recipe).
- Every patched file retains CRLF byte structure (asserted, 0 lone LFs).

### Still open (after this pass)

1. Frontend — globe label crowding and a full WCAG audit remain open; they need design
   decisions, not doc corrections.
2. Helm — an immutable-selector migration plan for existing releases (fresh installs are
   fixed; live Deployments need a resource-recreation path).
3. GitHub issue #78 remains open (verified live); it predates this pass and is tracked there.
